{
  "host": "${process.env.APP_HOST || '127.0.0.1'}",
  "port": "${process.env.PORT || 3001}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
